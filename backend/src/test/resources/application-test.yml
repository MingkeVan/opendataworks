# 测试环境配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/opendataworks?useUnicode=true&characterEncoding=utf8mb4&useSSL=false&serverTimezone=Asia/Shanghai
    username: opendataworks
    password: opendataworks123
    driver-class-name: com.mysql.cj.jdbc.Driver

# DolphinScheduler配置（直接调用 OpenAPI）
dolphin:
  url: ${DOLPHIN_URL:http://localhost:12345/dolphinscheduler}
  token: ${DOLPHIN_TOKEN:changeme}
  project-name: ${DOLPHIN_PROJECT_NAME:opendataworks}
  project-code: ${DOLPHIN_PROJECT_CODE:0}
  tenant-code: default
  worker-group: default
  execution-type: PARALLEL

# 日志配置
logging:
  level:
    com.onedata.portal: DEBUG
